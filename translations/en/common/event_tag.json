{
  "errors": {
    "nameRequired": "Tag name is required",
    "colorRequired": "Tag color is required",
    "deleteInUse": "Cannot delete tag — it's assigned to {{.count}} event(s). Deactivate instead or remove assignments first."
  }
}
